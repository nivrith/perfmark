import {
  Perfmark
} from './../src/index';
import {
  expect
} from 'chai';

describe('perfmark', () => {

  it('expects length of benchmark to be `0`', () => {
    let benchmark = new Perfmark();
    expect(benchmark.length).to.equal(0);
  });

  it

});
